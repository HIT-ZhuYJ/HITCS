### 1.

$$X(t) = A\cos\omega_0 t + B\sin\omega_0 t$$
$A,B \sim N(0,\sigma^2)$ 独立
证 $X(t)$ 为正态过程

---

### 2.

$$X(t) = At + B,\quad t\in(-\infty,+\infty)$$

1) $X(t)$ 均方连续
2) $Y(t) = \int_0^t X(s)ds$，求 $Y(t)$ 的 $m_Y$ 和 $R_Y(t_1,t_2)$

---

### 3.

$$X(t) = a\cos(\omega_0 t + \varphi),\quad \varphi\sim U(0,2\pi)$$

1) 平稳，求 $m_X, R_X(\tau)$
2) 期望、相关函数各态历经性

---

### 4.

$$S_X(\omega) = \frac{1}{(\omega^2+1)^2}$$
求 $R_X(\tau) = \frac{1}{4}e^{-|\tau|}(1+|\tau|)$

---

### 5.

$$P = \begin{bmatrix} 0.75 & 0.25 \\ 0.5 & 0.5 \end{bmatrix}$$
求 $P^{(n)},\ \lim_{n\to\infty}\pi_0^{(n)},\ \lim_{n\to\infty}\pi_1^{(n)}$

---

### 6.

$$
\begin{cases}
P_{0i} = P_i \\
P_{ii-1} = 1
\end{cases}
$$

1) 常返不可约
2) 若 $\sum_{n=1}^\infty P_n < +\infty,\ \sum_{n=1}^\infty nP_n < +\infty$
   证明正常返且平稳分布 $\pi_i = \frac{\sum_{n=i}^\infty P_n}{\sum_{n=1}^\infty nP_n}$

---

### 7.

写一种泊松过程定义
求 $M_X(t), R_X(t_1,t_2)$

---

